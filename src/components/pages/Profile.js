import { Fragment } from "react";
import Card from "../UI/Card";

const Profile = () => {
  return (
    <Fragment>
      <Card>
        <p> This is Profile page</p>
      </Card>
    </Fragment>
  );
};

export default Profile;
